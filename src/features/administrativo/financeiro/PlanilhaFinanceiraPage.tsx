import { PlaceholderPage } from "@/components/PlaceholderPage";

export function PlanilhaFinanceiraPage() {
  return (
    <PlaceholderPage
      titulo="Planilha Financeira"
      descricao="Consolidação das movimentações financeiras do ano, com exportação para PDF/CSV. Será construída sobre a API em uma próxima fase."
    />
  );
}
